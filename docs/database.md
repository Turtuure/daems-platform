# Database

**Database name:** `daems_db`
**Engine:** MySQL 8.x
**Default charset:** `utf8mb4` / `utf8mb4_unicode_ci`

All primary keys are `CHAR(36)` UUID v7 values generated by `Daems\Domain\Shared\ValueObject\Uuid7`. There are no `AUTO_INCREMENT` integer IDs.

---

## Migration index

Migrations live in `database/migrations/` and must be applied in numerical order.

| File                                        | Purpose                                              |
| ------------------------------------------- | ---------------------------------------------------- |
| `001_create_events_table.sql`               | `events` table                                       |
| `002_create_insights_table.sql`             | `insights` table                                     |
| `003_create_projects_table.sql`             | `projects` table                                     |
| `004_create_member_applications_table.sql`  | `member_applications` table                          |
| `005_create_supporter_applications_table.sql` | `supporter_applications` table                     |
| `006_create_users_table.sql`                | `users` table (initial schema)                       |
| `007_create_forum_tables.sql`               | `forum_categories`, `forum_topics`, `forum_posts`    |
| `008_add_role_to_users.sql`                 | Adds `role` column to `users`                        |
| `009_create_project_extras.sql`             | `project_participants`, `project_comments`, `project_updates` |
| `010_create_project_proposals.sql`          | `project_proposals` table                            |
| `011_extend_users_profile.sql`              | Adds address, membership, and member-number columns to `users` |
| `012_create_event_registrations.sql`        | `event_registrations` table                          |
| `013_add_user_id_to_forum.sql`              | Adds nullable `user_id` FK columns to `forum_topics` and `forum_posts` |
| `014_create_auth_tokens.sql`                | `auth_tokens` table (opaque bearer tokens, SHA-256 hash)               |
| `015_create_auth_login_attempts.sql`        | `auth_login_attempts` table (rate-limit counter)                       |
| `016_add_owner_id_to_projects.sql`          | Adds nullable `owner_id` FK column to `projects`                       |

### `auth_tokens`

Stores one row per active or expired session.

| Column         | Type           | Notes                                              |
| -------------- | -------------- | -------------------------------------------------- |
| `id`           | `CHAR(36)`     | UUIDv7                                             |
| `token_hash`   | `CHAR(64)`     | SHA-256 hex of the raw token; **unique**           |
| `user_id`      | `CHAR(36)`     | FK to `users.id`, `ON DELETE CASCADE`              |
| `issued_at`    | `DATETIME`     | Creation time; hard cap = `issued_at + 30 days`    |
| `last_used_at` | `DATETIME`     | Updated on every authenticated request             |
| `expires_at`   | `DATETIME`     | `LEAST(last_used_at + 7d, issued_at + 30d)`        |
| `revoked_at`   | `DATETIME`     | NULL while active; set on logout                   |
| `user_agent`   | `VARCHAR(255)` | Optional, recorded on issue                        |
| `ip`           | `VARCHAR(45)`  | Optional, recorded on issue                        |

The raw token never leaves the issuing response body; only the SHA-256 hash is persisted.

### `auth_login_attempts`

Rolling log of login attempts for rate limiting.

| Column         | Type           | Notes                                          |
| -------------- | -------------- | ---------------------------------------------- |
| `id`           | `BIGINT UNSIGNED` | `AUTO_INCREMENT`                            |
| `ip`           | `VARCHAR(45)`  | `REMOTE_ADDR` at the time of the attempt       |
| `email`        | `VARCHAR(255)` | Candidate email                                |
| `attempted_at` | `DATETIME`     |                                                |
| `success`      | `TINYINT(1)`   | 1 if authenticated, 0 otherwise                |

Index on `(ip, email, attempted_at)` supports the rate-limit window query. Opportunistic cleanup (1-in-100 insert sweep) removes rows older than 24 hours.

### `projects.owner_id`

`owner_id CHAR(36) NULL` added after `id`, with FK to `users.id` `ON DELETE SET NULL`. New projects always receive the creating user's id. Rows predating migration 016 have `owner_id IS NULL` and are only mutable by admins.

---

## Table schemas

### `events`

Stores public events (upcoming, past, online).

| Column        | Type                              | Nullable | Default             | Notes                     |
| ------------- | --------------------------------- | -------- | ------------------- | ------------------------- |
| `id`          | `CHAR(36)`                        | no       |                     | UUID7 PK                  |
| `slug`        | `VARCHAR(255)`                    | no       |                     | UNIQUE                    |
| `title`       | `VARCHAR(255)`                    | no       |                     |                           |
| `type`        | `ENUM('upcoming','past','online')`| no       |                     | Index: `events_type_date` |
| `event_date`  | `DATE`                            | no       |                     | Index: `events_type_date` |
| `event_time`  | `VARCHAR(50)`                     | yes      |                     |                           |
| `location`    | `VARCHAR(255)`                    | yes      |                     |                           |
| `is_online`   | `TINYINT(1)`                      | no       | `0`                 |                           |
| `description` | `TEXT`                            | yes      |                     |                           |
| `hero_image`  | `VARCHAR(500)`                    | yes      |                     |                           |
| `gallery_json`| `TEXT`                            | yes      |                     | JSON array of image paths |
| `created_at`  | `DATETIME`                        | no       | `CURRENT_TIMESTAMP` |                           |

---

### `event_registrations`

Tracks which users are registered for which events.

| Column          | Type       | Nullable | Default             | Notes                                |
| --------------- | ---------- | -------- | ------------------- | ------------------------------------ |
| `id`            | `CHAR(36)` | no       |                     | UUID7 PK                             |
| `event_id`      | `CHAR(36)` | no       |                     | FK → `events.id` ON DELETE CASCADE   |
| `user_id`       | `CHAR(36)` | no       |                     | FK → `users.id` ON DELETE CASCADE    |
| `registered_at` | `DATETIME` | no       | `CURRENT_TIMESTAMP` | UNIQUE on `(event_id, user_id)`      |

---

### `insights`

Editorial articles / thought-leadership pieces.

| Column           | Type           | Nullable | Default             | Notes                            |
| ---------------- | -------------- | -------- | ------------------- | -------------------------------- |
| `id`             | `CHAR(36)`     | no       |                     | UUID7 PK                         |
| `slug`           | `VARCHAR(255)` | no       |                     | UNIQUE                           |
| `title`          | `VARCHAR(255)` | no       |                     |                                  |
| `category`       | `VARCHAR(100)` | no       |                     | Index: `insights_category_date`  |
| `category_label` | `VARCHAR(100)` | no       |                     | Human-readable label             |
| `featured`       | `TINYINT(1)`   | no       | `0`                 |                                  |
| `published_date` | `DATE`         | no       |                     | Index: `insights_category_date`  |
| `author`         | `VARCHAR(255)` | no       |                     |                                  |
| `reading_time`   | `INT`          | no       | `0`                 | Minutes                          |
| `excerpt`        | `TEXT`         | no       |                     |                                  |
| `hero_image`     | `VARCHAR(500)` | yes      |                     |                                  |
| `tags_json`      | `TEXT`         | yes      |                     | JSON array of tag strings        |
| `content`        | `LONGTEXT`     | no       |                     | Full article body                |
| `created_at`     | `DATETIME`     | no       | `CURRENT_TIMESTAMP` |                                  |

---

### `projects`

Community project definitions.

| Column        | Type           | Nullable | Default             | Notes                              |
| ------------- | -------------- | -------- | ------------------- | ---------------------------------- |
| `id`          | `CHAR(36)`     | no       |                     | UUID7 PK                           |
| `slug`        | `VARCHAR(255)` | no       |                     | UNIQUE                             |
| `title`       | `VARCHAR(255)` | no       |                     |                                    |
| `category`    | `VARCHAR(100)` | no       |                     | Index: `projects_category_status`  |
| `icon`        | `VARCHAR(100)` | no       | `'bi-folder'`       | Bootstrap Icons class name         |
| `summary`     | `TEXT`         | no       |                     |                                    |
| `description` | `LONGTEXT`     | no       |                     |                                    |
| `status`      | `VARCHAR(50)`  | no       | `'active'`          | Index: `projects_category_status`  |
| `sort_order`  | `INT`          | no       | `0`                 |                                    |
| `created_at`  | `DATETIME`     | no       | `CURRENT_TIMESTAMP` |                                    |

---

### `project_participants`

Many-to-many join between users and projects.

| Column       | Type       | Nullable | Default             | Notes                                  |
| ------------ | ---------- | -------- | ------------------- | -------------------------------------- |
| `id`         | `CHAR(36)` | no       |                     | UUID7 PK                               |
| `project_id` | `CHAR(36)` | no       |                     | FK → `projects.id` ON DELETE CASCADE   |
| `user_id`    | `CHAR(36)` | no       |                     | FK → `users.id` ON DELETE CASCADE      |
| `joined_at`  | `DATETIME` | no       | `CURRENT_TIMESTAMP` | UNIQUE on `(project_id, user_id)`      |

---

### `project_comments`

User comments on a project.

| Column            | Type            | Nullable | Default             | Notes                                |
| ----------------- | --------------- | -------- | ------------------- | ------------------------------------ |
| `id`              | `CHAR(36)`      | no       |                     | UUID7 PK                             |
| `project_id`      | `CHAR(36)`      | no       |                     | FK → `projects.id` ON DELETE CASCADE |
| `user_id`         | `CHAR(36)`      | no       |                     | FK → `users.id` ON DELETE CASCADE    |
| `author_name`     | `VARCHAR(100)`  | no       |                     |                                      |
| `avatar_initials` | `VARCHAR(4)`    | no       | `''`                |                                      |
| `avatar_color`    | `VARCHAR(20)`   | no       | `''`                |                                      |
| `content`         | `TEXT`          | no       |                     |                                      |
| `likes`           | `INT UNSIGNED`  | no       | `0`                 |                                      |
| `created_at`      | `DATETIME`      | no       | `CURRENT_TIMESTAMP` |                                      |

---

### `project_updates`

Project status mini-blog entries posted by project members.

| Column        | Type           | Nullable | Default             | Notes                                |
| ------------- | -------------- | -------- | ------------------- | ------------------------------------ |
| `id`          | `CHAR(36)`     | no       |                     | UUID7 PK                             |
| `project_id`  | `CHAR(36)`     | no       |                     | FK → `projects.id` ON DELETE CASCADE |
| `title`       | `VARCHAR(200)` | no       |                     |                                      |
| `content`     | `TEXT`         | no       |                     |                                      |
| `author_name` | `VARCHAR(100)` | no       |                     |                                      |
| `created_at`  | `DATETIME`     | no       | `CURRENT_TIMESTAMP` |                                      |

---

### `project_proposals`

Community-submitted project proposals awaiting review.

| Column         | Type           | Nullable | Default             | Notes                              |
| -------------- | -------------- | -------- | ------------------- | ---------------------------------- |
| `id`           | `CHAR(36)`     | no       |                     | UUID7 PK                           |
| `user_id`      | `CHAR(36)`     | no       |                     | FK → `users.id` ON DELETE CASCADE  |
| `author_name`  | `VARCHAR(100)` | no       |                     |                                    |
| `author_email` | `VARCHAR(200)` | no       |                     |                                    |
| `title`        | `VARCHAR(200)` | no       |                     |                                    |
| `category`     | `VARCHAR(100)` | no       |                     |                                    |
| `summary`      | `VARCHAR(300)` | no       |                     |                                    |
| `description`  | `TEXT`         | no       |                     |                                    |
| `status`       | `VARCHAR(30)`  | no       | `'pending'`         |                                    |
| `created_at`   | `DATETIME`     | no       | `CURRENT_TIMESTAMP` |                                    |

---

### `users`

Registered user accounts. Schema built across migrations 006, 008, and 011.

| Column             | Type           | Nullable | Default        | Notes                     |
| ------------------ | -------------- | -------- | -------------- | ------------------------- |
| `id`               | `CHAR(36)`     | no       |                | UUID7 PK                  |
| `name`             | `VARCHAR(255)` | no       |                |                           |
| `email`            | `VARCHAR(255)` | no       |                | UNIQUE                    |
| `password_hash`    | `VARCHAR(255)` | no       |                | bcrypt / password_hash()  |
| `date_of_birth`    | `DATE`         | no       |                |                           |
| `role`             | `VARCHAR(30)`  | no       | `'registered'` | Added by migration 008    |
| `country`          | `VARCHAR(100)` | no       | `''`           | Added by migration 011    |
| `address_street`   | `VARCHAR(200)` | no       | `''`           | Added by migration 011    |
| `address_zip`      | `VARCHAR(20)`  | no       | `''`           | Added by migration 011    |
| `address_city`     | `VARCHAR(100)` | no       | `''`           | Added by migration 011    |
| `address_country`  | `VARCHAR(100)` | no       | `''`           | Added by migration 011    |
| `membership_type`  | `VARCHAR(30)`  | no       | `'individual'` | Added by migration 011    |
| `membership_status`| `VARCHAR(30)`  | no       | `'active'`     | Added by migration 011    |
| `member_number`    | `VARCHAR(30)`  | yes      | `NULL`         | Added by migration 011    |
| `created_at`       | `DATETIME`     | no       | `CURRENT_TIMESTAMP` |                      |

---

### `forum_categories`

Top-level discussion categories.

| Column        | Type           | Nullable | Default | Notes  |
| ------------- | -------------- | -------- | ------- | ------ |
| `id`          | `CHAR(36)`     | no       |         | UUID7 PK |
| `slug`        | `VARCHAR(120)` | no       |         | UNIQUE |
| `name`        | `VARCHAR(120)` | no       |         |        |
| `icon`        | `VARCHAR(60)`  | no       | `''`    |        |
| `description` | `TEXT`         | no       |         |        |
| `sort_order`  | `SMALLINT`     | no       | `0`     |        |

---

### `forum_topics`

Discussion threads within a category.

| Column            | Type           | Nullable | Default | Notes                                       |
| ----------------- | -------------- | -------- | ------- | ------------------------------------------- |
| `id`              | `CHAR(36)`     | no       |         | UUID7 PK                                    |
| `category_id`     | `CHAR(36)`     | no       |         | FK → `forum_categories.id`                  |
| `user_id`         | `CHAR(36)`     | yes      | NULL    | FK (nullable); added by migration 013        |
| `slug`            | `VARCHAR(160)` | no       |         | UNIQUE                                      |
| `title`           | `VARCHAR(255)` | no       |         |                                             |
| `author_name`     | `VARCHAR(120)` | no       |         |                                             |
| `avatar_initials` | `VARCHAR(4)`   | no       | `''`    |                                             |
| `avatar_color`    | `VARCHAR(20)`  | yes      |         |                                             |
| `pinned`          | `TINYINT(1)`   | no       | `0`     |                                             |
| `reply_count`     | `INT`          | no       | `0`     |                                             |
| `view_count`      | `INT`          | no       | `0`     |                                             |
| `last_activity_at`| `DATETIME`     | no       |         |                                             |
| `last_activity_by`| `VARCHAR(120)` | no       | `''`    |                                             |
| `created_at`      | `DATETIME`     | no       |         |                                             |

---

### `forum_posts`

Individual replies within a topic.

| Column            | Type           | Nullable | Default | Notes                               |
| ----------------- | -------------- | -------- | ------- | ----------------------------------- |
| `id`              | `CHAR(36)`     | no       |         | UUID7 PK                            |
| `topic_id`        | `CHAR(36)`     | no       |         | FK → `forum_topics.id`              |
| `user_id`         | `CHAR(36)`     | yes      | NULL    | FK (nullable); added by migration 013 |
| `author_name`     | `VARCHAR(120)` | no       |         |                                     |
| `avatar_initials` | `VARCHAR(4)`   | no       | `''`    |                                     |
| `avatar_color`    | `VARCHAR(20)`  | yes      |         |                                     |
| `role`            | `VARCHAR(60)`  | no       | `''`    |                                     |
| `role_class`      | `VARCHAR(60)`  | no       | `''`    |                                     |
| `joined_text`     | `VARCHAR(60)`  | no       | `''`    |                                     |
| `content`         | `TEXT`         | no       |         |                                     |
| `likes`           | `INT`          | no       | `0`     |                                     |
| `sort_order`      | `INT`          | no       | `0`     |                                     |
| `created_at`      | `DATETIME`     | no       |         |                                     |

---

### `member_applications`

Individual membership applications pending admin review.

| Column          | Type           | Nullable | Default             | Notes                       |
| --------------- | -------------- | -------- | ------------------- | --------------------------- |
| `id`            | `CHAR(36)`     | no       |                     | UUID7 PK                    |
| `name`          | `VARCHAR(255)` | no       |                     |                             |
| `email`         | `VARCHAR(255)` | no       |                     | Index: `member_apps_email`  |
| `date_of_birth` | `DATE`         | no       |                     |                             |
| `country`       | `VARCHAR(100)` | yes      |                     |                             |
| `motivation`    | `TEXT`         | no       |                     |                             |
| `how_heard`     | `VARCHAR(50)`  | yes      |                     |                             |
| `status`        | `VARCHAR(20)`  | no       | `'pending'`         | Index: `member_apps_status` |
| `created_at`    | `DATETIME`     | no       | `CURRENT_TIMESTAMP` |                             |

---

### `supporter_applications`

Organisational supporter applications pending admin review.

| Column           | Type           | Nullable | Default             | Notes                           |
| ---------------- | -------------- | -------- | ------------------- | ------------------------------- |
| `id`             | `CHAR(36)`     | no       |                     | UUID7 PK                        |
| `org_name`       | `VARCHAR(255)` | no       |                     |                                 |
| `contact_person` | `VARCHAR(255)` | no       |                     |                                 |
| `reg_no`         | `VARCHAR(100)` | yes      |                     |                                 |
| `email`          | `VARCHAR(255)` | no       |                     | Index: `supporter_apps_email`   |
| `country`        | `VARCHAR(100)` | yes      |                     |                                 |
| `motivation`     | `TEXT`         | no       |                     |                                 |
| `how_heard`      | `VARCHAR(50)`  | yes      |                     |                                 |
| `status`         | `VARCHAR(20)`  | no       | `'pending'`         | Index: `supporter_apps_status`  |
| `created_at`     | `DATETIME`     | no       | `CURRENT_TIMESTAMP` |                                 |

---

## Entity Relationship Diagram

```
users
  |
  |--< event_registrations >-- events
  |
  |--< project_participants >-- projects
  |                                |
  |--< project_comments            |--< project_comments
  |                                |
  |--< project_proposals           |--< project_updates
  |
  |--< forum_topics (user_id nullable)
  |        |
  |        |--< forum_posts (user_id nullable)
  |
  forum_categories
       |
       |--< forum_topics
                |
                |--< forum_posts

member_applications     (no FK to users — submitted before account creation)
supporter_applications  (no FK to users — submitted before account creation)

insights   (standalone, no FK relationships)
```

### Cardinalities

| Relationship                              | Type   |
| ----------------------------------------- | ------ |
| user → event_registrations                | 1 : N  |
| event → event_registrations               | 1 : N  |
| user ↔ project (via project_participants) | M : N  |
| project → project_comments                | 1 : N  |
| user → project_comments                  | 1 : N  |
| project → project_updates                 | 1 : N  |
| project → project_proposals               | —      |
| user → project_proposals                  | 1 : N  |
| forum_category → forum_topics             | 1 : N  |
| forum_topic → forum_posts                 | 1 : N  |
